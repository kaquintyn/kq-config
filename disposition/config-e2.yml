---
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: customer
disposition:
  call-result:
    translation:
      OOCA : Answer
      OOCB : DroppedCall
      OOCD : WrongParty
logging:
  level:
    org.springframework.web: ERROR
    com.customer: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file: /Users/kaquintyn/Documents/Development/application.log
...